import { fetchPokemonApi } from './Query/pokemon';
import type   { SpeciesResolvers } from './../../types.generated';
    export const Species: Pick<SpeciesResolvers, 'name'|'url'> = {
    /* Implement Species resolver logic here */

  };